{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "..",
    "types": ["node", "vitest/globals"]
  },
  "include": [
    "./**/*.ts",
    "../app.ts",
    "../config/**/*.ts",
    "../modules/**/*.ts"
  ],
  "exclude": [
    "../../dist",
    "../../node_modules"
  ]
}
