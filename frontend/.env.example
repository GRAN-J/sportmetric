# Fuente de datos del frontend:
# - local: usa los JSON del proyecto
# - api: consume el backend
VITE_DATA_SOURCE=local

# URL base del backend cuando se usa VITE_DATA_SOURCE=api
VITE_API_BASE_URL=http://localhost:3001
